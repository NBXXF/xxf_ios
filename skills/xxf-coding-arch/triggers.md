# xxf-coding-arch — 触发用例

## 应该触发

- "给这个模块补一下分层设计"
- "这段代码是否违反项目架构约束"
- "路由层直接调数据库可以吗"
- "Repository 和 ViewModel 的边界怎么划"

## 不应该触发

- "Swift 语法怎么写" → 应走基础语言说明
- "某个 API 参数是什么" → 应走对应模块 skill

## 边界用例

- "我想临时在 VC 里直接发网络请求"
  - 期望：触发，并指出偏离架构边界与替代方案
